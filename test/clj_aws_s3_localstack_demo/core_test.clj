(ns clj-aws-s3-localstack-demo.core-test
  (:require [clojure.test :refer :all]
            [clj-aws-s3-localstack-demo.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
